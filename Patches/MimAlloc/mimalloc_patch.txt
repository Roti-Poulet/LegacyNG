diff --git a/src/init.c b/src/init.c
index ca8982c..f7029a2 100644
--- a/src/init.c
+++ b/src/init.c
@@ -320,8 +320,8 @@ static void mi_theap_main_init(void) {
   if mi_unlikely(mi_theap_main.memid.memkind != MI_MEM_STATIC) {
     // theap
     mi_theap_main.memid = _mi_memid_create(MI_MEM_STATIC);
-    #if defined(__APPLE__) || (defined(_WIN32) && !defined(MI_SHARED_LIB))
-      _mi_random_init_weak(&mi_theap_main.random);    // prevent allocation failure during bcrypt dll initialization with static linking (issue #1185)
+    #if defined(__APPLE__) || defined(_WIN32)
+      _mi_random_init_weak(&mi_theap_main.random);
     #else
       _mi_random_init(&mi_theap_main.random);
     #endif
@@ -1068,7 +1068,9 @@ void _mi_auto_process_init(void) {
   }
 
   // reseed random
+  #if !defined(_WIN32)
   _mi_random_reinit_if_weak(&mi_theap_main.random);
+  #endif
 }
 
 // CPU features
diff --git a/src/prim/windows/prim.c b/src/prim/windows/prim.c
index 2787738..c9b7ab7 100644
--- a/src/prim/windows/prim.c
+++ b/src/prim/windows/prim.c
@@ -715,9 +715,17 @@ bool _mi_prim_random_buf(void* buf, size_t buf_len) {
 typedef LONG (NTAPI *PBCryptGenRandom)(HANDLE, PUCHAR, ULONG, ULONG);
 static  PBCryptGenRandom pBCryptGenRandom = NULL;
 
+#if defined(_MSC_VER)
+static __declspec(thread) bool mi_prim_random_buf_active = false;
+#else
+static __thread bool mi_prim_random_buf_active = false;
+#endif
+
 bool _mi_prim_random_buf(void* buf, size_t buf_len) {
   mi_assert(buf_len <= ULONG_MAX);
   if (buf_len > ULONG_MAX) return false;
+  if (mi_prim_random_buf_active) return false;
+  mi_prim_random_buf_active = true;
   mi_atomic_do_once {
     HINSTANCE hDll = mi_win_loadlibrary(TEXT("bcrypt.dll"));
     if (hDll != NULL) {
@@ -725,8 +733,12 @@ bool _mi_prim_random_buf(void* buf, size_t buf_len) {
       // mi_win_freelibrary(hDll);  // don't free
     }
   }
-  if (pBCryptGenRandom == NULL) return false;
-  return (pBCryptGenRandom(NULL, (PUCHAR)buf, (ULONG)buf_len, BCRYPT_USE_SYSTEM_PREFERRED_RNG) >= 0);
+  bool ok = false;
+  if (pBCryptGenRandom != NULL) {
+    ok = (pBCryptGenRandom(NULL, (PUCHAR)buf, (ULONG)buf_len, BCRYPT_USE_SYSTEM_PREFERRED_RNG) >= 0);
+  }
+  mi_prim_random_buf_active = false;
+  return ok;
 }
 
 #endif  // MI_USE_RTLGENRANDOM
